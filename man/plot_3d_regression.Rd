% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_3d_regression.R
\name{plot_3d_regression}
\alias{plot_3d_regression}
\title{Interactive 3D scatterplot with a fitted regression plane}
\usage{
plot_3d_regression(data, formula, n = 25)
}
\arguments{
\item{data}{a data frame containing the three variables in \code{formula}.}

\item{formula}{a formula of the form \code{z ~ x + y}: a single outcome on
the left-hand side and exactly two predictor variables on the
right-hand side. In-formula transformations (e.g. \code{log(z) ~ x + y})
are not supported in this version — apply transformations to \code{data}
beforehand.}

\item{n}{grid resolution per axis used to draw the regression plane.
Default \code{25}.}
}
\value{
A \code{\link[plotly:plotly]{plotly}} htmlwidget object.
}
\description{
Build an interactive 3D scatterplot of an outcome against two numeric
predictors, overlaid with the fitted plane from \code{lm(formula, data)}.
Returns a \code{\link[plotly:plotly]{plotly}} htmlwidget that you can render in
an R Markdown / Quarto document or in the RStudio Viewer.
}
\details{
Requires the suggested package \code{plotly}. Install it with
\code{install.packages("plotly")} if you see an error about it being missing.
}
\examples{
\dontrun{
  library(moderndive)
  plot_3d_regression(
    un_member_states_2024,
    life_expectancy_2022 ~ gdp_per_capita + fertility_rate_2022
  )
}
}
\seealso{
\code{\link[=get_regression_table]{get_regression_table()}}, \code{\link[=get_regression_points]{get_regression_points()}}
}
