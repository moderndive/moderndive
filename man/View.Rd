% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view.R
\name{View}
\alias{View}
\title{View a data object with a clearer error in non-interactive contexts}
\usage{
View(x, title = NULL)
}
\arguments{
\item{x}{an \R object which can be coerced to a data frame with
non-zero numbers of rows and columns.}

\item{title}{title for the viewer window. Defaults to the name of \code{x}
prefixed by \verb{Data: }, matching the convention used by \code{\link[utils:View]{utils::View()}}.}
}
\value{
Invisibly returns \code{NULL}. Called for the side effect of
displaying \code{x} in a viewer.
}
\description{
A drop-in replacement for \code{\link[utils:View]{utils::View()}} that emits an actionable error
when called from a non-interactive context such as an R Markdown or
Quarto document, rather than the cryptic platform-specific errors
\code{utils::View()} produces. Behaves identically to \code{utils::View()} in an
interactive R session.
}
\examples{
\dontrun{
  # In an interactive R session, behaves like utils::View():
  View(mtcars)
}
}
\seealso{
\code{\link[utils:View]{utils::View()}}
}
